SUPABASE_URL=""
SUPABASE_ANON_KEY=""

# The port the server will listen on
# Options: Any valid port number (default: 3000)
PORT=3000

# The application environment
# Options: development, production, test
NODE_ENV=development

# The origin allowed to make requests to the API (CORS)
# Options: http://localhost:3000 (for local development), or your production URL
ALLOWED_ORIGIN=http://localhost:3000
